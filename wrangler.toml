# Cloudflare Pages project configuration.
# `wrangler pages deploy` reads the project name and output dir from here.
name = "lady-diet"
compatibility_date = "2026-09-15"
pages_build_output_dir = "dist"
